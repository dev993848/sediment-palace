# 05. FAQ

## Where is data stored?
In the local project directory under `memory/`.

## Does this require cloud services?
No. SedimentPalace is local-first.

## How do I check runtime health?
Use `healthcheck` and `get_metrics`.

## Should I run `recover_journal` periodically?
Not usually. Run it after crashes or interrupted operations.
